# fsrv - A dead simple http file server

    Usage: fsrv [OPTIONS] <ROOT>

    Arguments:
      <ROOT>

    Options:
          --host <HOST>  [default: 127.0.0.1]
          --port <PORT>  [default: 80]
      -h, --help         Print help
